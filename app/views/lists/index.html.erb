<h1>My Movie Lists</h1>
<%= link_to 'Create a New List', new_list_path %>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>  <!-- Link to each list's show page -->
    </li>
  <% end %>
</ul>
